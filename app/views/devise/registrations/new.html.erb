<div class="container">
  <div class="my-5">
    <h1>ようこそ、Mathmateへ！</h1>
    <h2 class="explain">アカウント登録（無料）して、数学を楽しもう！</h2>
    
    <hr>

    <div class="row">
      <div class="left-content col-md-6 col-sm-12">
      
          <div class="mt-4 p-3" style="background-color: #7fbfff;">
          【採用担当者の方へ】<br>
          お疲れさまです。ご覧いただきありがとうございます。<br>
          ログイン画面にテスト用のアカウントを用意していますので、ぜひログインしてみてください。
          <div>
            <%= link_to new_user_session_path  do%>
                <div class="btn btn-info btn-block mt-4 mb-2 shadow"><%= icon("fa", "home") %> ログイン</div>
            <% end %>
          </div>
          </div>

      </div>

      <div class="right-content col-md-6 col-sm-12">
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= bootstrap_devise_error_messages! %>
          <div class="form-group container mx-auto my-3">
            <%= f.label :email %>
            <%= f.email_field :email, autofocus: true, autocomplete: 'email', class: 'form-control shadow' %>
          </div>
          <div class="form-group container mx-auto my-3">
            <%= f.label :name %>
            <%= f.text_field :name, autofocus: true, autocomplete: 'name', class: 'form-control shadow', maxlength: '30' %>
          </div>
          <div class="form-group container my-3">
            <%= f.label :password %>
            <%= f.password_field :password, autocomplete: 'current-password', class: 'form-control shadow'%>
            <small class="form-text text-muted">パスワードは英数字記号を1つずつ、6文字以上12文字以下で設定してね！</small>
          </div>
          <div class="form-group container mx-auto my-3">
            <%= f.submit t('.sign_up'), class: 'btn btn-primary btn-block' %>
          </div>
        <% end %>
        <%= render 'devise/shared/links' %>
      </div>  
    </div>
  </div>
</div>